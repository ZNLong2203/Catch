# Khoá Gemini — lấy ở https://aistudio.google.com/apikey
# Không bao giờ để lọt vào trình duyệt: mọi lệnh gọi đi qua route handler phía máy chủ,
# và lib/gemini.server.ts mở đầu bằng `import 'server-only'`.
GEMINI_API_KEY=
